import express from "express";import cors from "cors";const app=express();app.use(cors());app.use(express.json());const PORT=process.env.PORT||3000;app.post('/api/chat',async(req,res)=>{try{const{message}=req.body;const apiKey=(process.env.OPENAI_API_KEY||'').trim();if(!apiKey)return res.json({reply:'Error: Falta la OPENAI_API_KEY en Railway.'});const response=await fetch('https://openrouter.ai/api/v1/chat/completions',{method:'POST',headers:{'Authorization':`Bearer ${apiKey}`,'Content-Type':'application/json'},body:JSON.stringify({model:'mistralai/mistral-7b-instruct',messages:[{role:'user',content:message}]})});const data=await response.json();if(data.error||!data.choices||!data.choices[0])return res.json({reply:'Error OpenRouter: '+(data.error?.message||'Sin respuesta')});res.json({reply:data.choices[0].message.content})}catch(e){res.status(500).json({reply:'Error: '+e.message})}});app.get("/",(req,res)=>{res.send(`<!DOCTYPE html><html><head><meta charset="UTF-8"><title>XIA CLEAN</title></head><body><h1>XIA CLEAN</h1><textarea id="m"></textarea><button onclick="f()">Enviar</button><div id="r"></div><script>async function f(){const m=document.getElementById('m').value;const r=document.getElementById('r');const d=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:m})});const j=await d.json();r.innerText=j.reply}</script></body></html>`)});app.listen(PORT,'0.0.0.0',()=>{console.log(`OK ${PORT}`)});console.log(`OK ${PORT}`)});
